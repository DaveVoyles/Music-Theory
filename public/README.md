# `public/` — static assets

Copied as-is into Vite `dist/` root (honoring `base` path).

| File | Role |
|------|------|
| `favicon.svg` | Site icon |
| `icons.svg` | Scaffold leftover icons (safe to prune if unused) |

Add new static files here only when they must be addressable by URL without bundling through JS.
