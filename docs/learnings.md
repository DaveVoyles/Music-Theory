# Learnings

## 2026-07-25 — Plan 0001 orchestration (interactive music theory app)

- **Single-orchestrator loop worked** across D1–D10: assign-first claim, treehouse lease, review-lens receipt + `land-pr.sh`, board card moves.
- **`@pixi/react` Text typings** were awkward (anchor prop conflict); imperative Pixi `Application` for CoF + fretboard was the practical path while keeping PixiJS v8.
- **`land-pr` requires App receipt** on HEAD; hand-assembled soft-lens findings + `review-lens-receipt.sh` were sufficient on this private repo (no branch protection).
- **GitHub Pages** workflow publishes to `gh-pages` successfully; public site URL still needs Pages source enabled and public/Pro if the repo stays private.
- **D1 self-merge** used personal `gh pr merge` before the land-pr path was established; later slices used Gatekeeper/App receipts correctly.
